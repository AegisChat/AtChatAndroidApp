<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <padding
        android:left="7dp"
        android:top="15dp"
        android:right="7dp"
        android:bottom="15dp" />
    <stroke
        android:width="2px"
        android:color="@color/AtChatYellow"
        />
    <!-- View background color -->
    <solid
        android:color="@color/AtChatDarkGrey" >
    </solid>

    <!-- The radius makes the corners rounded -->
    <corners
        android:radius="6dp">
    </corners>
</shape>